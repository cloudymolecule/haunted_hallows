<h1> <%= @booking.name %> </h1>

Guest: <%= current_user.username %></br>
Room: <%= @room.number %></br></br>

<% if @booking.guest_id == current_user.id %>
    <%= link_to "Edit Booking", edit_booking_path(@booking) %> ||
    <%= link_to "Delete Booking", booking_path(@booking), method: 'delete' %></br>
<% end %>
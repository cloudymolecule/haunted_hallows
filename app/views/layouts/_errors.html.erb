<% if object.errors.any? %>
    <h3><%= pluralize(object.errors.count, "error") %> prevented from being saved: </h3>
    <ul>
        <% object.errors.full_messages.each do |m| %>
            <li><%= m %></li>
        <% end %>
    </ul>
<% end %>